type Mutation {
  createAccount(
    username: String!
    email: String!
    firstName: String
    lastName: String
    bio: String
    loginSecret: String!
  ): User!
}

type Query {
  something: String!
}
